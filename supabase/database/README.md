# Deprecated

Schema source of truth moved to:

```text
packages/db/src/schema.ts
```

Sync with:

```bash
pnpm db:sync
```

These SQL files are kept only as historical reference and are no longer used by the sync script.
